# This workflow has been superseded by ci.yml
# It is kept here only to avoid breaking any external badge URLs.
# See .github/workflows/ci.yml for the current CI configuration.

name: Node CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

jobs:
  redirect:
    name: See ci.yml
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI has moved to ci.yml — this job is a placeholder only."
